import { BackgroundContentDirective } from './background-content.directive';

describe('BackgroundContentDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundContentDirective();
    expect(directive).toBeTruthy();
  });
});
